ou are refactoring a Next.js 14 (App Router) + Tailwind + TypeScript + Framer Motion site for Neo Automatics. Implement ALL changes precisely.
0) Non-negotiables
No pages must 404. Routes required: /, /capabilities, /industries, /quality, /products, /about, /contact, /privacy, /terms.
Keep copy buyer-focused: quality • cost • reliability • delivery.
A11y & perf: contrast ≥ 4.5:1, semantic headings, visible focus rings, Lighthouse ≥ 95 on Home (production build).
1) New professional theme (no “sports drink” gradients)
Replace all current colors with Steel & Graphite system (Tailwind tokens in tailwind.config):
--bg-base: #0B0C0E (charcoal)
--bg-elevated: #111316
--text-primary: #E8EAF0
--text-muted: #A3A7B2
Accents (used sparingly, no gradients):
--accent-amber: #F0B429 (industrial amber for CTAs, small highlights)
--accent-oxide: #D04A3A (oxide red for secondary emphasis & error states)
Surfaces / dividers: ring-1 ring-white/10, separators #2A2F36
Buttons:
Primary: amber fill, subtle outer glow on hover (no gradient)
Secondary: outline in #5A5F6B, hover darken background bg-white/5
Remove green/purple/orange gradients; stick to solid, muted, industrial.
2) Hero restructure + auto-parts animation
Hero H1: “Neo Automatics”
Sub-headline under H1: “Precision Machined Components for OEMs & Tier-1s”
Supporting line: “End-to-end machining, heat treatment & QA—delivered at scale.”
CTAs: Primary → /contact (“Request a Quote”), Secondary → /neo-capability-profile.pdf
Background animation (professional, lightweight):
Create /components/AutoPartsBackdrop.tsx: SVG layer with gears, bolts, washers in wireframe style.
Animate using CSS keyframes (slow rotate, gentle drift, 0.2–0.6 opacity). No heavy libraries.
Respect prefers-reduced-motion: if true → render static SVG.
Place it absolutely behind hero, add a subtle bg-black/40 overlay for contrast.
3) Products page — fix “View Quote” UX (must convert)
Goal: When a purchaser clicks “View Details & Quote” on any product, it should open a clean, high-conversion modal with details + quote form.
Implement:
A reusable ProductQuoteModal component.
Prefill product name, category, image.
Form fields (validated on client + API):
Name*, Company*, Work Email*, Phone
Product* (prefilled, read-only)
Annual Volume*, Material, Surface Finish, Target Price (optional)
Drawing Upload (PDF/DWG, ≤10MB)
Message
Submit → /api/rfq (Nodemailer with env: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, TO_EMAIL=neo.automatics@gmail.com).
Fallback to Formspree if SMTP missing (FORMSPREE_ID).
Success UX: success banner + link “Upload more drawings” + “Back to Products”.
Failure UX: clear error state with guidance.
Scroll lock the body when modal open; ensure z-50 so it never sits under page content.
Make the “View Details & Quote” buttons consistent across all product cards (no overlap, no weird shadows).
Also:
Add “Download spec sheet (PDF)” optional link (placeholder) within the modal.
Support query param ?product=XYZ to open modal directly (deep link from marketing emails).
4) Polish & layout fixes
Tighten vertical rhythm: sections py-20 md:py-28, headings tracking-tight, body leading-relaxed.
Reduce oversized shadows; prefer shadow-[0_10px_30px_rgba(0,0,0,0.35)] only on elevated modals.
Unify card radii to rounded-2xl; apply ring-1 ring-white/10.
Replace any remaining color classes not in theme tokens.
Ensure navbar links are visible at top at all breakpoints; active link shows amber underline.
Fix any overlapping elements on /products (no absolute elements covering content).
Add aria-* labels on all buttons, inputs, and modal close.
Run production build & verify no TypeScript/ESLint errors.
5) Data integration (still needed)
Load from /data/company.json:
name: "Neo Automatics", email: "neo.automatics@gmail.com", phone: "+91 88130 79855", owner: "Kushagra Dhingra"
machines.CNC = 30, certifications ["ISO 9001:2015","PPAP Ready"], products list, etc.
6) Acceptance criteria
Hero shows Neo Automatics with the auto-parts animation behind it (wireframe gears/bolts). Looks professional and subtle, not flashy.
The entire site uses the Steel & Graphite palette; no gradients and no greens/purples/blues.
“View Details & Quote” opens a high-quality modal with prefilled product name, proper form, file upload, validation, and clear success/failure states.
No element overlap on /products and no 404s anywhere.
Lighthouse ≥ 95 for Perf/A11y/Best/SEO on Home (production build).