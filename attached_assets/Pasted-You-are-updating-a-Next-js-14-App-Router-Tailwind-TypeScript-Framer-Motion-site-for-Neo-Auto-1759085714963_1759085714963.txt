You are updating a Next.js 14 (App Router) + Tailwind + TypeScript + Framer Motion site for Neo Automatics. Replace the current yellow/amber theme with a professional Silicon-Valley palette and apply it consistently across the app.
1) Theme: Graphite + Electric-Teal (no yellow)
In tailwind.config (or global CSS variables), define these tokens and refactor components to use them:
// tailwind.config.{ts,js} - extend theme with CSS variables or direct colors
extend: {
  colors: {
    bg: {
      base: "#0B0C0F",      // page background (charcoal)
      elevated: "#111317",  // cards/nav
    },
    text: {
      primary: "#E8ECF2",   // main text
      muted: "#A6AABC",     // secondary text
      subtle: "#7E8496",
    },
    accent: {
      primary: "#13E3B3",   // electric-teal (primary accent)
      soft: "#22BFA2",      // softer teal for hovers/badges
    },
    border: "#262B33",      // dividers/rings
    overlay: "rgba(0,0,0,0.5)" // hero/video overlay
  },
  boxShadow: {
    card: "0 10px 30px rgba(0,0,0,0.35)",
    glow: "0 0 24px rgba(19,227,179,0.20)" // teal glow
  },
  borderRadius: {
    card: "1rem"
  }
}
Global rules
Remove all yellow/amber colors and classes.
Use bg-bg-base, bg-bg-elevated, text-text-primary, text-text-muted.
CTA color = accent.primary (teal). Hover: slight brightness + shadow-glow.
Secondary buttons: outline with border-border + hover bg-white/5.
Underlines/active nav indicators use accent.soft.
Keep gradients very subtle: if needed, use from-[#0B0C0F] to-[#0F1216] only (no color gradients).
2) Components to refactor
Navbar: background bg-bg-elevated/80 backdrop-blur, active link underline text-accent-primary border-b-2 border-accent-primary.
Hero: keep your auto-parts animation; add bg-overlay layer to ensure 4.5:1 contrast. Headline in text-text-primary, subline in text-text-muted.
Buttons:
Primary: bg-accent-primary text-black hover:brightness-110 shadow-glow
Secondary: border border-border text-text-primary hover:bg-white/5
Cards (capabilities/products): bg-bg-elevated ring-1 ring-border rounded-card shadow-card hover:shadow-glow/0 (tiny glow on hover).
Badges/Pills: background bg-white/5 text text-text-primary or text-text-muted, accent text text-accent-primary.
Icons: set stroke to #C5CAD6 by default; accent icons can use accent.primary sparingly.
Links: default text-text-primary with underline on hover in accent.soft.
3) Products → Quote Modal (keep UX)
Ensure the “View Details & Quote” modal inherits the new palette:
Modal wrapper bg-bg-elevated ring-1 ring-border shadow-card.
Primary submit button uses teal variant.
Focus rings focus:ring-2 focus:ring-accent.soft.
Check z-index (z-50) and scroll-lock remain intact.
4) Cleanup & consistency
Grep for old palette classes (amber, yellow, previous hex codes) and replace with new tokens.
Remove any leftover gradient classes that introduce color noise.
Verify contrast on headlines over hero/video (bg-overlay ≥ rgba(0,0,0,0.55) if needed).
Update any SVG strokes/fills that were yellow to neutral/teal.
Ensure favicon/OG image still fit the new palette (neutral with a hint of teal).
5) QA / Acceptance
No yellow/amber remains anywhere.
All pages use Graphite + Electric-Teal system consistently.
CTAs feel premium (teal fill, subtle glow, no neon).
Lighthouse (prod build) ≥ 95 for Perf/A11y/Best/SEO.
No TypeScript/ESLint errors, no console warnings.
Now implement all changes.