You are refactoring a Next.js 14 + Tailwind + TypeScript site for Neo Automatics. Replace the current green theme with a clean, enterprise-grade BLUE palette and apply it across all components without breaking layout or accessibility.
1) Theme tokens (Tailwind)
Add/replace in tailwind.config (or global CSS vars) and refactor components to use these tokens only:
extend: {
  colors: {
    bg: { base: "#0A0F1A", elevated: "#0F1522" },      // deep navy / panel
    text: { primary: "#E6ECF8", muted: "#A9B3C7", subtle: "#8894AA" },
    blue: {
      50:  "#EAF3FF",
      100: "#D5E7FF",
      400: "#63A3FF",
      500: "#3B82F6",   // primary accent (accessible)
      600: "#2662D9",
      700: "#1C4FB3"
    },
    border: "#22304A",
    overlay: "rgba(4,10,20,0.55)" // hero/video overlay
  },
  boxShadow: {
    card: "0 10px 30px rgba(0,0,0,0.35)",
    glow: "0 0 28px rgba(59,130,246,0.28)"
  },
  borderRadius: { card: "1rem" }
}
Rules
Remove all green/teal tokens. No inline color hexes—use tokens above.
Background gradient (very subtle) for hero: from-[#0A0F1A] via-[#0B1220] to-[#0D1526].
Primary interactive color = blue.500; hover → blue.600; focus ring → ring-blue-400/60.
Secondary buttons: neutral outline (border-border) + hover bg-white/5.
2) Global refactor
Navbar: bg-bg-elevated/80 backdrop-blur with active link underline border-b-2 border-blue-500.
Hero: keep your auto-parts background; add bg-overlay to maintain 4.5:1 contrast.
H1 text-text-primary, H2 text-text-muted.
Primary CTA uses blue.500, slight shadow-glow on hover.
Cards (capabilities/products): bg-bg-elevated ring-1 ring-border rounded-card shadow-card hover:shadow-glow.
Badges/Pills: bg-white/5 text-text-primary with label text in blue.400.
Links: default text-text-primary → on hover text-blue-400 + underline.
3) Components to update
Buttons:
Primary: bg-blue-500 text-white hover:bg-blue-600 focus:ring-2 focus:ring-blue-400/60 shadow-glow.
Secondary: border border-border text-text-primary hover:bg-white/5.
Quote Modal + RFQ form: inputs keep neutral look; focus ring blue.400; submit uses Primary.
Ask-Neo chat widget: header bar bg-bg-elevated, send button Primary.
Metrics & icons: neutral strokes #C8D0E0; accent highlights blue.400 (sparingly).
4) Accessibility & performance
Ensure contrast ≥ 4.5:1 on hero/subheads/buttons; if needed, bump overlay to bg-black/60.
Respect prefers-reduced-motion.
Remove any leftover green classes; search & replace to theme tokens.
5) Acceptance tests
No green/teal remains anywhere. All CTAs use blue.500/600.
Navbar active link has blue underline; hover states consistent.
Product/Capabilities/Quality pages show the blue system on cards, badges, and buttons.
Lighthouse (prod) ≥ 95 Perf/A11y/Best/SEO; no console errors.
Now implement all changes.